// npm-install-example index.js
const mySimplePackage = require("rgap-simple-package");
console.log(mySimplePackage());

/**
 * This example demonstrates the following:
 * - Installing a local package using npm install
 * - Using the installed package in a project
 *
 * To run this example:
 * 1. Ensure you have the package.json file with rgap-simple-package listed as a dependency.
 * 2. Run `npm install` to install the dependencies.
 * 3. Run this script using `npm start` or `node index.js`.
 */
